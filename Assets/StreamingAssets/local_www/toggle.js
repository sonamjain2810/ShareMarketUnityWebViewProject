// JavaScript Document
$(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect1" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button1" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect2" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button2" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect3" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button3" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect4" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button4" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect5" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button5" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect6" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button6" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect7" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button7" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect8" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button8" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect9" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button9" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect10" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button10" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect11" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button11" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect12" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button12" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect13" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button13" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect14" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button14" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect15" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button15" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect16" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button16" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect17" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button17" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect18" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button18" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect19" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button19" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect20" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button20" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect21" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button21" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect22" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button22" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect23" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button23" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect24" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button24" ).click(function() {
            runEffect();
            return false;
        });
    });
    $(function() {
        // run the currently selected effect
        function runEffect() {
            // get effect type from
            var selectedEffect = "blind";
 
            // most effect types need no options passed by default
            var options = {};
            // some effects have required parameters
            if ( selectedEffect === "scale" ) {
                options = { percent: 0 };
            } else if ( selectedEffect === "size" ) {
                options = { to: { width: 200, height: 60 } };
            }
 
            // run the effect
            $( "#effect25" ).toggle( selectedEffect, options, 500 );
        };
 
        // set effect from select menu value
        $( "#button25" ).click(function() {
            runEffect();
            return false;
        });
    });
    